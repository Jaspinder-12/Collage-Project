## 2026-03-16 - Improve Form Accessibility
**Learning:** Implementing visual indicators for required fields and proper focus states using vanilla CSS ensures better keyboard navigation and meets user expectations for accessible forms.
**Action:** Use `label[for]::after` to append required indicators and `:focus`/`:focus-visible` combined with `box-shadow` for accessible, visually pleasing outlines across the app.
