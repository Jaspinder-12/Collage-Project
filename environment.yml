name: base
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - flask
      - pytest
      - joblib
      - numpy
      - scikit-learn
      - pandas
      - requests
      - flake8
      - playwright
