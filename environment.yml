name: collage-project
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - flask
      - joblib
      - numpy
      - scikit-learn
      - pandas
      - pytest
      - pytest-mock
      - requests
      - flake8
