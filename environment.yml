name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - flask
  - joblib
  - numpy
  - scikit-learn
  - pandas
  - pytest
  - requests
  - flake8
  - pip
  - pip:
    - playwright
