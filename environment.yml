name: base
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - flask
    - joblib
    - numpy
    - scikit-learn
    - pandas
    - pytest
    - requests
    - flake8
    - playwright