## 2024-05-24 - Form Input Types and Labels
**Learning:** Found that adding HTML5 specific types (`type="number"`) to inputs makes data entry much easier and adds implicit validation, improving accessibility. Adding `required` attribute helps ensure fields aren't skipped.
**Action:** Always prefer semantic HTML5 input types (like `number` with `step="any"`) for numerical data.
